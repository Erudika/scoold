use scoold;

update column family Users
    with comparator = UTF8Type
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 20000
    and rows_cached = 20000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and compression_options={sstable_compression:SnappyCompressor, chunk_length_kb:64}
    and memtable_throughput = 64;

update column family Schools
    with comparator = UTF8Type
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 10000
    and rows_cached = 10000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and compression_options={sstable_compression:SnappyCompressor, chunk_length_kb:64}
    and memtable_throughput = 64;

update column family Classes
    with comparator = UTF8Type
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 10000
    and rows_cached = 10000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and compression_options={sstable_compression:SnappyCompressor, chunk_length_kb:64}
    and memtable_throughput = 64;

update column family Posts
    with comparator = UTF8Type
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 10000
    and rows_cached = 5000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and compression_options={sstable_compression:SnappyCompressor, chunk_length_kb:64}
    and memtable_throughput = 64;

update column family Reports
    with comparator = UTF8Type
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 1000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and compression_options={sstable_compression:SnappyCompressor, chunk_length_kb:64}
    and memtable_throughput = 64;

update column family Revisions
    with comparator = UTF8Type
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 10000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and compression_options={sstable_compression:SnappyCompressor, chunk_length_kb:64}
    and memtable_throughput = 64;

update column family Comments
    with comparator = UTF8Type
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 100000
    and rows_cached = 100000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and compression_options={sstable_compression:SnappyCompressor, chunk_length_kb:64}
    and memtable_throughput = 64;

update column family Media
    with comparator = UTF8Type
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 10000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and compression_options={sstable_compression:SnappyCompressor, chunk_length_kb:64}
    and memtable_throughput = 64;

update column family Messages
    with comparator = UTF8Type
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 10000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and compression_options={sstable_compression:SnappyCompressor, chunk_length_kb:64}
    and memtable_throughput = 64;

update column family Tags
    with comparator = UTF8Type
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 5000
    and rows_cached = 5000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and compression_options={sstable_compression:SnappyCompressor, chunk_length_kb:64}
    and memtable_throughput = 64;

update column family Translations
    with comparator = UTF8Type
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 5000
    and rows_cached = 5000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and compression_options={sstable_compression:SnappyCompressor, chunk_length_kb:64}
    and memtable_throughput = 64;

update column family UsersUuids
    with comparator = UTF8Type
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 5000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family SchoolsUuids
    with comparator = UTF8Type
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 5000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family ClassesUuids
    with comparator = UTF8Type
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 5000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family PostsUuids
    with comparator = UTF8Type
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 5000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family ReportsUuids
    with comparator = UTF8Type
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 5000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family RevisionsUuids
    with comparator = UTF8Type
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 5000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family CommentsUuids
    with comparator = UTF8Type
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 5000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family MediaUuids
    with comparator = UTF8Type
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 5000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family MessagesUuids
    with comparator = UTF8Type
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 5000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family RevisionsParentUuids
    with comparator = LongType
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 5000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family CommentsParentUuids
    with comparator = LongType
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 5000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family MediaParentUuids
    with comparator = LongType
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 5000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family MessagesParentUuids
    with comparator = LongType
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 5000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family Counters
    with comparator = UTF8Type
	and default_validation_class = CounterColumnType
    and key_validation_class = UTF8Type
    and keys_cached = 100
	and replicate_on_write = true;

update column family Votes
    with comparator = UTF8Type
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 5000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family AuthKeys
    with comparator = UTF8Type
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 10000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family UserAuth
    with comparator = UTF8Type
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 10000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family Emails
    with comparator = UTF8Type
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 10000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family UsersByReputation
    with comparator = UTF8Type
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 5000
    and rows_cached = 5000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family UsersByTimestamp
    with comparator = LongType
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 5000
    and rows_cached = 5000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family Contacts
    with comparator = LongType
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 5000
    and rows_cached = 10000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family UserSchools
    with comparator = LongType
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 10000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family UserClasses
    with comparator = LongType
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 10000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family UserQuestions
    with comparator = LongType
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 10000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family UserAnswers
    with comparator = LongType
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 10000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family NewMessages
    with comparator = LongType
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 10000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family Photos
    with comparator = LongType
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 10000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family Drawer
    with comparator = LongType
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 10000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family Labels
    with comparator = UTF8Type
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 5000
    and rows_cached = 5000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family LabelsMedia
    with comparator = LongType
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 5000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family SchoolClasses
    with comparator = LongType
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 10000
    and rows_cached = 10000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family SchoolUsers
    with comparator = LongType
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 10000
    and rows_cached = 10000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family SchoolsByTimestamp
    with comparator = LongType
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 5000
    and rows_cached = 5000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family SchoolsByVotes
    with comparator = UTF8Type
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 5000
    and rows_cached = 5000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family ClassUsers
    with comparator = LongType
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 10000
    and rows_cached = 10000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family ClassesByTimestamp
    with comparator = LongType
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 5000
    and rows_cached = 5000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family Questions
    with comparator = LongType
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 5000
    and rows_cached = 5000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family QuestionsByVotes
    with comparator = UTF8Type
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 5000
    and rows_cached = 5000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family QuestionsByActivity
    with comparator = LongType
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 5000
    and rows_cached = 5000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family Answers
    with comparator = LongType
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 5000
    and rows_cached = 5000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family AnswersByVotes
    with comparator = UTF8Type
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 5000
    and rows_cached = 5000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family AllQuestions
    with comparator = LongType
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 5000
    and rows_cached = 5000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family AllQuestionsByVotes
    with comparator = UTF8Type
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 5000
    and rows_cached = 5000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family AllQuestionsByActivity
    with comparator = LongType
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 5000
    and rows_cached = 5000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family Feedback
    with comparator = LongType
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 5000
    and rows_cached = 5000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family FeedbackByVotes
    with comparator = UTF8Type
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 5000
    and rows_cached = 5000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family FeedbackByActivity
    with comparator = LongType
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 5000
    and rows_cached = 5000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family ReportsByTimestamp
    with comparator = LongType
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 5000
    and rows_cached = 5000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family LocalesTranslations
    with comparator = UTF8Type
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 5000
    and rows_cached = 5000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family ApprovedTranslations
    with comparator = UTF8Type
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 5000
    and rows_cached = 5000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;

update column family Language
    with comparator = UTF8Type
    and default_validation_class = UTF8Type
    and key_validation_class = UTF8Type
    and keys_cached = 5000
    and rows_cached = 5000
    and gc_grace = 18000
    and key_cache_save_period = 3600
    and memtable_throughput = 64;