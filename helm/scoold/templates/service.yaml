apiVersion: v1
kind: Service
metadata:
  name: {{ include "scoold.fullname" . }}
  labels:
    {{- include "scoold.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: {{ .Values.service.name }}
  selector:
    {{- include "scoold.selectorLabels" . | nindent 4 }}
